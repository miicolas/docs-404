---
title: Introduction à React
description: Découvrez ce qu'est React, pourquoi l'utiliser et les concepts clés pour bien démarrer dans l'écosystème React.
---

# Introduction à React pour débutants

React est une bibliothèque JavaScript développée par Facebook (maintenant Meta) qui a révolutionné la façon dont nous construisons des interfaces utilisateur pour le web. Si vous débutez dans le développement web moderne, vous avez fait un excellent choix en commençant par React !

## Qu'est-ce que React ?

React est une **bibliothèque JavaScript** (et non un framework complet) qui se concentre sur une chose : construire des interfaces utilisateur (UI) efficaces et réutilisables.

Les principes fondamentaux de React incluent :

- **Une approche déclarative** : Vous décrivez à quoi devrait ressembler votre interface en fonction de l'état actuel, et React s'occupe de mettre à jour le DOM efficacement.
- **Basé sur des composants** : Vous construisez des interfaces en assemblant des composants indépendants et réutilisables.
- **Apprendre une fois, écrire partout** : Que vous développiez pour le web (React), le mobile (React Native) ou même la réalité virtuelle (React 360), les concepts restent similaires.

## Pourquoi utiliser React ?

<Cards>
<Card icon="🧩" title="Composants réutilisables">
  Divisez votre interface en petites briques logiques et réutilisables
</Card>
<Card icon="⚡️" title="DOM virtuel">
  Optimise les performances grâce à une mise à jour efficace du DOM
</Card>
<Card icon="🌐" title="Large adoption">
  Utilisé par Facebook, Instagram, Netflix, Airbnb, et bien d'autres
</Card>
<Card icon="🧠" title="Facile à apprendre">
  Une courbe d'apprentissage accessible pour les débutants
</Card>
</Cards>

## Concepts clés à comprendre

Pour bien débuter avec React, vous devez comprendre quelques concepts fondamentaux :

### 1. JSX : JavaScript + XML

React utilise JSX, une extension de syntaxe pour JavaScript qui ressemble à du HTML :

```jsx
const element = <h1>Bonjour React !</h1>;
```

JSX n'est ni une chaîne de caractères, ni du HTML - c'est une syntaxe qui sera transformée en JavaScript standard par les outils de build.

### 2. Les Composants

Les composants sont au cœur de React. Un composant peut être aussi simple qu'une fonction qui retourne du JSX :

```jsx
function Salutation() {
  return <h1>Bonjour, le monde !</h1>;
}
```

### 3. Props (Propriétés)

Les props permettent de passer des données d'un composant parent à un composant enfant :

```jsx
function Salutation(props) {
  return <h1>Bonjour, {props.nom} !</h1>;
}

// Utilisation : <Salutation nom="Alice" />
```

### 4. État (State)

L'état permet aux composants de gérer et de stocker des données qui peuvent changer au fil du temps :

```jsx
import { useState } from 'react';

function Compteur() {
  const [compteur, setCompteur] = useState(0);
  
  return (
    <div>
      <p>Vous avez cliqué {compteur} fois</p>
      <button onClick={() => setCompteur(compteur + 1)}>
        Cliquez-moi
      </button>
    </div>
  );
}
```

## Prérequis pour ce cours

Pour tirer le maximum de ce cours, vous devriez avoir :

- **Des connaissances de base en HTML, CSS et JavaScript**
- **Un environnement de développement** (Node.js et npm installés)
- **Un éditeur de code**

Si certains de ces prérequis vous manquent, ne vous inquiétez pas ! Nous fournirons des liens vers des ressources complémentaires tout au long du cours.

## Ce que vous allez apprendre

Dans ce cours de plusieurs chapitres, vous allez découvrir :

1. **Comment configurer un projet React avec Vite**, un outil de build ultrarapide
2. **Les bases des composants React** et comment les structurer
3. **La gestion de l'état et des événements** pour rendre vos interfaces interactives
4. **Comment communiquer entre composants** avec les props
5. **Les Hooks fondamentaux** (useState, useEffect, etc.)
6. **Le styling en React** avec du CSS standard
7. **La gestion des formulaires** et la validation
8. **Un projet pratique complet** : une application de liste de tâches

## Comment suivre ce cours

Nous vous recommandons de suivre ce cours dans l'ordre, en codant vous-même les exemples présentés. La pratique est essentielle pour bien assimiler les concepts.

Chaque chapitre se construit sur les connaissances acquises dans les chapitres précédents, avec des exemples concrets et des exercices pratiques.

Prêt à commencer votre voyage dans l'univers de React ? Passons au chapitre suivant pour installer notre environnement de développement avec Vite ! 